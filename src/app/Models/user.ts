export interface User {
    id?: number; // optionnel lors de la création
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    username: string;
    password: string;
  }